import React from 'react';
import styled from "styled-components";

const App = (props) => {

    return (
        <Wrapper>
           App
        </Wrapper>
    )
};

const Wrapper = styled.div`
`;

export default App;