import {takeLatest, call, put} from '@redux-saga/core/effects';
import {Action} from "./redux";

export default function*() {
    //ex
    // yield takeLatest(Action.Types.UPDATE_STATE, function*(){
    //     const result = yield call()
    // });
}